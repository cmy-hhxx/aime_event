# 修复方案的泛化性分析与时间外推实证

> 问题：漏斗的噪声过滤规则是在 2025-07~2026-06 这一年数据上"打地鼠"迭代出来的，
> 拿到近几年数据上跑还有效吗？
> 实证脚本：`scripts/template_miner.py`；结果：`reports/funnel_experiment/miner_full.json`

## 1. 先拆解：现有修复里什么泛化、什么不泛化

| 组件 | 泛化性 | 理由 |
|---|---|---|
| 漏斗架构（过滤→聚类→阈值→triage） | ✅ 机制级 | 与时间窗无关 |
| 稀有词锚定连边（防模板链） | ✅ 机制级 | ~~绝对阈值 df≤2000~~ **已改为池大小相对比例**，跨语料自适应 |
| L2 tags / 长度 / 精确去重 | ✅ schema 级 | 只要数据 schema 稳定就有效（但 schema 本身要监控，见 §4） |
| LLM triage 显式负类 | ✅ 语义级 | 判"模板流/例行公告/数据播报"不依赖措辞 |
| **L3 手写正则（7 类）** | ⚠️ **部分** | `est.`/`y/y`/`target price`/`Market Talk` 是几十年不变的财经行文惯例，大概率跨期稳定；但 Zacks 系栏目名（"Laps the Stock Market"）是随媒体产品变化的 |
| MACRO_TOPICS 通用桶（fed/通胀/关税） | ✅ | 词汇跨期稳定 |
| MACRO_TOPICS 专名桶（stargate/genius act） | ❌ | 2025-26 专有名词，跑 2022 数据没用，跑 2027 数据缺新词 |
| n≥5 送审门槛 | ⚠️ | 新闻密度随年代变化（现有管道的 `EVENT_ERA_SPLIT` 已是先例），须按窗口 sweep |

**结论：架构泛化，规则不完全泛化。** 所以泛化的正确单位不是"这套规则"，
而是"**获得这套规则的流程**"。为此做了一个数据驱动的替代/补充机制并实证其跨期表现：

## 2. 模板骨架挖掘：数据驱动、零人工、每窗口自适应

```text
骨架 = 标题掩码(数字→#, 大写词串→@)   例: "Fitch affirms Iowa's IDR at AAA" → "@ affirms @ at @"
模板 = 骨架满足 [出现≥20次 + 不同主体≥10 + 头部来源占比≥60% + 固定词≥3]
```

判据的直觉：**模板流 = 同一家自动化栏目把同一句式套在很多主体上**。
来源集中度是关键防误杀项——"@ to acquire @ for $#"（真并购句式）来源分散，不会被判模板。

## 3. 时间外推实证（H1 挖 → H2 验证）

数据切半：H1 = 2025-07~12，H2 = 2026-01~06（H2 共 1,072,540 条）。

| 指标 | 数值 | 解读 |
|---|---:|---|
| H1 挖出模板 | 411 个 | 全自动，跑一遍 ~7 分钟 |
| H2 原生模板（oracle） | 419 个 | |
| **跨期保持率** | **82.1%**（24,063/29,326） | 半年前挖的模板在新窗口仍能带走 8 成该杀记录 |
| 误杀抽检 | **40/40 全是噪声** | 数据播报/评级/大宗交易/指数播报，零误杀 |
| 漂移新模板的 H2 命中 | 7,582 条 | 且 top15 漂移骨架多为已知家族变体（"raises target price to # from #" 的措辞变位），真正的新噪声体裁很少 |

**与手写正则在 H2 上的对比**（重要，防止过度乐观）：

| | 骨架挖掘(T1) | 手写正则 |
|---|---:|---:|
| H2 杀伤量 | 24,063（2.2%） | 207,480（19.3%） |
| 独有杀伤 | 11,285 | 194,702 |

挖掘是**精确骨架匹配**，正则是**家族级匹配**（一条 `est\.` 覆盖千百种骨架变体），
所以挖掘不能取代正则的量，两者互补：挖掘负责自动捕捉"新出现的栏目模板"（正则的盲区），
正则负责家族级通杀（挖掘的盲区）。

## 4. 跑近几年数据的操作方案（三层防线 + 两个监控）

**三层噪声防线**（维护成本递减，泛化性递增）：

1. **惯例正则**（Tier 2，低维护）：保留 7 类通用规则；每个新时间窗跑一次"校准循环"——
   `funnel_experiment.py` 全量 4.5 分钟/轮，看 top25 大簇补规则，2-3 轮收敛
   （本次实践：53,598→9,159，两轮共补 4 类规则）。这个循环本身已脚本化，是可复制的流程。
2. **骨架挖掘**（Tier 1，零维护）：每个窗口自动重挖（约 7 分钟），82% 跨期保持意味着
   即使懒到不重挖，旧模板库也能带走大部分；重挖则自动跟上媒体产品的变化。
3. **triage 显式负类**（Tier 3，零维护）：语义级兜底，把"模板流/例行公告/数据播报/追踪流"
   写进拒绝类别。前两层的作用是把送审量压到 triage 花得起的规模。

**两个监控**（发现"不泛化"正在发生）：

- **逐层计数表**：Pass A 每层淘汰率是数据画像的指纹。换一批数据后某层淘汰率突变
  （如 L2 tags 命中率跌到 0）= schema 或内容分布漂移的告警。
  本次的活例子：这份导出 NEWS 的 `entities.stocks` 全空，就是靠逐条核对发现的——
  跑新数据前先跑 Pass A 看层计数，比跑完全程才发现问题便宜得多。
- **日历召回验收**：FOMC/CPI/NFP 日期任何年份都是公开已知的，
  对任何时间窗都可以做"这些日子附近必须聚出簇"的召回检查，跨年免费可用。

**常量自适应**（已落地）：

- `RARE_DF_MAX` 已改为池大小的相对比例（`RARE_DF_RATIO=0.0014`）；
- 送审门槛 n≥K：每窗口用 `--sweep` 式阈值表定，不硬编码（老年份新闻稀,K 应更低,
  与现有管道 `EVENT_ERA_SPLIT` 的早年 n≥2 逻辑一致）。

## 5. 直接回答

**手写规则本身：部分泛化**（财经行文惯例类稳定，媒体栏目名类会过期）。
**方案整体：泛化**，因为它不押注规则不变，而是把"规则的获得"变成了三件可复制的事：
① 每窗口 7 分钟自动挖模板（实测跨期保持率 82%、抽检零误杀）；
② 每窗口 2-3 轮、每轮 4.5 分钟的脚本化校准循环；
③ triage 语义兜底 + 层计数/日历两个漂移监控。
拿到近几年数据：先跑 Pass A 看层计数核 schema → 重挖模板 → 跑校准循环 → sweep 定阈值 → 送审。
