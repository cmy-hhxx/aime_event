{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CleanedRecord",
  "description": "Full audit record produced by the AIME event cleaning pipeline. Optional fields are omitted when source data is empty; public outputs never use null.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "content_type", "title", "published_at", "dedup"],
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1,
      "description": "Stable source record id from raw _id."
    },
    "content_type": {
      "type": "string",
      "enum": ["US_NEWS", "US_ROBOT", "US_ARTICLE", "US_FLASH", "US_POST", "US_NOTICE"],
      "description": "Business content type from raw businessCode."
    },
    "type_code": {
      "type": "integer",
      "description": "Original numeric raw type, kept for audit compatibility."
    },
    "title": {
      "type": "string",
      "description": "Trimmed source title."
    },
    "body": {
      "type": "string",
      "minLength": 1,
      "description": "Plain text converted from raw HTML content. Omitted for attachment-only notices."
    },
    "summary": {
      "type": "string",
      "minLength": 1,
      "description": "Source summary when present."
    },
    "published_at": {
      "type": "string",
      "format": "date-time",
      "description": "UTC ISO timestamp derived from raw ctime."
    },
    "updated_at": {
      "type": "string",
      "format": "date-time",
      "description": "UTC ISO timestamp derived from raw rtime, falling back to published_at when missing."
    },
    "source": {
      "type": "object",
      "additionalProperties": false,
      "minProperties": 1,
      "description": "Source attribution from raw news/source/author fields. Empty source fields are omitted.",
      "properties": {
        "name": { "type": "string", "minLength": 1, "description": "Publisher or source name." },
        "url": { "type": "string", "minLength": 1, "description": "Original source URL, not dedup-normalized." },
        "author": { "type": "string", "minLength": 1, "description": "Source author when present." }
      }
    },
    "entities": {
      "type": "object",
      "additionalProperties": false,
      "minProperties": 1,
      "description": "Entities extracted from source link metadata, not model-generated NER.",
      "properties": {
        "stocks": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/definitions/stockEntity" }
        },
        "crypto": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/definitions/cryptoEntity" }
        }
      }
    },
    "tags": {
      "type": "array",
      "minItems": 1,
      "description": "Raw non-numeric classification tags after light filtering.",
      "items": { "type": "string", "minLength": 1 }
    },
    "importance": {
      "type": "string",
      "enum": ["high", "mid", "low"],
      "description": "Derived from us_high_importance/us_mid_importance/us_low_importance tags."
    },
    "regions": {
      "type": "array",
      "minItems": 1,
      "description": "Region tags derived from contentTagMap.",
      "items": { "type": "string", "minLength": 1 }
    },
    "notice": {
      "type": "object",
      "additionalProperties": false,
      "minProperties": 1,
      "description": "SEC notice metadata for US_NOTICE when present.",
      "properties": {
        "filing_type": { "type": "string", "minLength": 1, "description": "Source noticeInfo.noticeType." },
        "declare_date": { "type": "string", "minLength": 1, "description": "Source noticeInfo.declareDate." },
        "attachments": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/definitions/noticeAttachment" }
        }
      }
    },
    "dedup": {
      "type": "object",
      "additionalProperties": false,
      "required": ["version", "key", "method", "is_canonical", "canonical_id", "debug"],
      "description": "Dedup decision metadata. Exact and high-confidence near-duplicate decisions are auditable here.",
      "properties": {
        "version": { "type": "integer", "enum": [4] },
        "key": { "type": "string", "minLength": 1, "description": "Normalized dedup key." },
        "method": {
          "type": "string",
          "enum": ["source_url", "content_hash", "notice_attachment", "near_minhash", "id"]
        },
        "is_canonical": { "type": "boolean" },
        "canonical_id": { "type": "string", "minLength": 1 },
        "debug": { "$ref": "#/definitions/dedupDebug" }
      }
    },
    "meta": {
      "type": "object",
      "additionalProperties": false,
      "minProperties": 1,
      "description": "Source-system provenance fields. Empty provenance fields are omitted.",
      "properties": {
        "language": { "type": "string", "minLength": 1, "description": "Raw language." },
        "gid": { "type": "string", "minLength": 1, "description": "Raw news.gid material group id." },
        "material_id": { "type": "string", "minLength": 1, "description": "Raw materialId." },
        "uid": { "type": "string", "minLength": 1, "description": "Raw extensions.uid." }
      }
    }
  },
  "definitions": {
    "stockEntity": {
      "type": "object",
      "additionalProperties": false,
      "required": ["symbol", "name"],
      "properties": {
        "symbol": { "type": "string", "minLength": 1 },
        "name": { "type": "string", "minLength": 1 }
      }
    },
    "cryptoEntity": {
      "type": "object",
      "additionalProperties": false,
      "required": ["code", "name"],
      "properties": {
        "code": { "type": "string", "minLength": 1 },
        "name": { "type": "string", "minLength": 1 }
      }
    },
    "noticeAttachment": {
      "type": "object",
      "additionalProperties": false,
      "required": ["url"],
      "properties": {
        "url": { "type": "string", "minLength": 1 },
        "file_type": { "type": "string", "minLength": 1 }
      }
    },
    "dedupDebug": {
      "type": "object",
      "additionalProperties": false,
      "required": ["version", "source"],
      "properties": {
        "version": { "type": "integer", "enum": [4] },
        "source": { "type": "string", "minLength": 1 },
        "source_url": { "type": "string", "minLength": 1 },
        "normalized_url": { "type": "string", "minLength": 1 },
        "rejected_url_reason": { "type": "string", "minLength": 1 },
        "hash_algorithm": { "type": "string", "minLength": 1 },
        "text_normalization": { "type": "string", "minLength": 1 },
        "notice_accession": { "type": "string", "minLength": 1 },
        "notice_attachment_url": { "type": "string", "minLength": 1 },
        "cluster_id": { "type": "string", "minLength": 1 },
        "minhash_threshold": { "type": "number" },
        "fuzzy_threshold": { "type": "number" },
        "scores": { "$ref": "#/definitions/nearScores" },
        "reason": { "type": "string", "minLength": 1 }
      }
    },
    "nearScores": {
      "type": "object",
      "additionalProperties": false,
      "required": ["minhash_jaccard", "fuzzy_score", "title_score"],
      "properties": {
        "minhash_jaccard": { "type": "number" },
        "fuzzy_score": { "type": "number" },
        "title_score": { "type": "number" }
      }
    }
  }
}
