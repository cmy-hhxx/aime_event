{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "EventRecord",
  "description": "LLM 事件抽取输出格式。一行对应一条 cleaned 输入，events 中保存 0 到多个事件。",
  "type": "object",
  "additionalProperties": true,
  "required": ["source_id", "source_file", "source_line", "model", "events"],
  "properties": {
    "source_id": {
      "type": "string",
      "minLength": 1,
      "description": "cleaned 输入记录 ID。"
    },
    "source_file": {
      "type": "string",
      "minLength": 1,
      "description": "来源 cleaned_batch 文件名。"
    },
    "source_line": {
      "type": "integer",
      "minimum": 1,
      "description": "来源文件内行号。"
    },
    "source_title": {
      "type": "string",
      "description": "来源标题。"
    },
    "published_at": {
      "type": "string",
      "description": "来源发布时间。"
    },
    "model": {
      "type": "string",
      "minLength": 1,
      "description": "抽取模型名。"
    },
    "events": {
      "type": "array",
      "items": { "$ref": "#/definitions/extractedEvent" }
    },
    "error": {
      "type": "string",
      "description": "抽取失败类型。"
    },
    "message": {
      "type": "string",
      "description": "抽取失败信息。"
    }
  },
  "definitions": {
    "extractedEvent": {
      "type": "object",
      "additionalProperties": true,
      "required": ["event_id", "event_type", "event_title"],
      "properties": {
        "event_id": { "type": "string", "minLength": 1 },
        "event_type": { "type": "string", "minLength": 1 },
        "event_title": { "type": "string", "minLength": 1 },
        "event_time": { "type": ["string", "null"] },
        "entities": { "type": "array" },
        "summary": { "type": "string" },
        "evidence": { "type": "string" },
        "confidence": { "type": "number", "minimum": 0, "maximum": 1 }
      }
    }
  }
}
