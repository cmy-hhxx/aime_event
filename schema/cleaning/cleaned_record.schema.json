{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CleanedRecord",
  "description": "AIME 事件清洗流水线的完整审计格式。可选字段在源数据为空时省略；公开输出不使用 null。",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "content_type", "title", "published_at", "dedup"],
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1,
      "description": "稳定记录 ID，来自原始 _id。"
    },
    "content_type": {
      "type": "string",
      "enum": ["US_NEWS", "US_ROBOT", "US_ARTICLE", "US_FLASH", "US_POST", "US_NOTICE"],
      "description": "业务内容类型，来自原始 businessCode。US_NEWS=新闻，US_ROBOT=机器生成，US_ARTICLE=文章，US_FLASH=快讯，US_POST=帖子，US_NOTICE=SEC 公告。"
    },
    "type_code": {
      "type": "integer",
      "description": "原始数值 type 字段，保留用于审计兼容。"
    },
    "title": {
      "type": "string",
      "description": "去首尾空白后的标题。"
    },
    "body": {
      "type": "string",
      "minLength": 1,
      "description": "由 HTML 转换的纯文本正文。仅附件公告可省略。"
    },
    "summary": {
      "type": "string",
      "minLength": 1,
      "description": "源数据摘要，有值时输出。"
    },
    "published_at": {
      "type": "string",
      "format": "date-time",
      "description": "UTC ISO 时间戳，来自原始 ctime。"
    },
    "updated_at": {
      "type": "string",
      "format": "date-time",
      "description": "UTC ISO 时间戳，来自原始 rtime；缺失时回退到 published_at。"
    },
    "source": {
      "type": "object",
      "additionalProperties": false,
      "minProperties": 1,
      "description": "来源信息，来自原始 news/source/author。空字段省略。",
      "properties": {
        "name": { "type": "string", "minLength": 1, "description": "发布方或来源名称。" },
        "url": { "type": "string", "minLength": 1, "description": "原始来源 URL，不做去重规范化。" },
        "author": { "type": "string", "minLength": 1, "description": "作者，有值时输出。" }
      }
    },
    "entities": {
      "type": "object",
      "additionalProperties": false,
      "minProperties": 1,
      "description": "从源链接元数据提取的实体，非模型 NER 生成。",
      "properties": {
        "stocks": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/definitions/stockEntity" }
        },
        "crypto": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/definitions/cryptoEntity" }
        }
      }
    },
    "tags": {
      "type": "array",
      "minItems": 1,
      "description": "轻量过滤后的原始分类标签。",
      "items": { "type": "string", "minLength": 1 }
    },
    "importance": {
      "type": "string",
      "enum": ["high", "mid", "low"],
      "description": "重要性，由 us_high/mid/low_importance 标签推导。"
    },
    "regions": {
      "type": "array",
      "minItems": 1,
      "description": "地区标签，来自 contentTagMap。",
      "items": { "type": "string", "minLength": 1 }
    },
    "notice": {
      "type": "object",
      "additionalProperties": false,
      "minProperties": 1,
      "description": "US_NOTICE 的 SEC 公告元数据。",
      "properties": {
        "filing_type": { "type": "string", "minLength": 1, "description": "公告类型 noticeInfo.noticeType。" },
        "declare_date": { "type": "string", "minLength": 1, "description": "申报日期 noticeInfo.declareDate。" },
        "attachments": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/definitions/noticeAttachment" }
        }
      }
    },
    "dedup": {
      "type": "object",
      "additionalProperties": false,
      "required": ["version", "key", "method", "is_canonical", "canonical_id", "debug"],
      "description": "去重决策元数据，精确与近似去重结果均可审计。",
      "properties": {
        "version": { "type": "integer", "enum": [4], "description": "去重规则版本，当前固定为 4。" },
        "key": { "type": "string", "minLength": 1, "description": "规范化后的去重键。" },
        "method": {
          "type": "string",
          "enum": ["source_url", "content_hash", "notice_attachment", "near_minhash", "id"],
          "description": "去重方法：source_url=同源 URL，content_hash=标题+正文哈希，notice_attachment=公告附件 URL，near_minhash=近似去重合并，id=重复原始 ID。"
        },
        "is_canonical": { "type": "boolean", "description": "true=保留记录（canonical），false=重复记录。" },
        "canonical_id": { "type": "string", "minLength": 1, "description": "指向保留记录的 ID；canonical 记录指向自身。" },
        "debug": { "$ref": "#/definitions/dedupDebug" }
      }
    },
    "meta": {
      "type": "object",
      "additionalProperties": false,
      "minProperties": 1,
      "description": "源系统溯源字段，空值省略。",
      "properties": {
        "language": { "type": "string", "minLength": 1, "description": "原始 language。" },
        "gid": { "type": "string", "minLength": 1, "description": "原始 news.gid 素材组 ID。" },
        "material_id": { "type": "string", "minLength": 1, "description": "原始 materialId。" },
        "uid": { "type": "string", "minLength": 1, "description": "原始 extensions.uid。" }
      }
    }
  },
  "definitions": {
    "stockEntity": {
      "type": "object",
      "additionalProperties": false,
      "required": ["symbol", "name"],
      "properties": {
        "symbol": { "type": "string", "minLength": 1, "description": "股票代码。" },
        "name": { "type": "string", "minLength": 1, "description": "股票名称。" }
      }
    },
    "cryptoEntity": {
      "type": "object",
      "additionalProperties": false,
      "required": ["code", "name"],
      "properties": {
        "code": { "type": "string", "minLength": 1, "description": "加密货币代码。" },
        "name": { "type": "string", "minLength": 1, "description": "加密货币名称。" }
      }
    },
    "noticeAttachment": {
      "type": "object",
      "additionalProperties": false,
      "required": ["url"],
      "properties": {
        "url": { "type": "string", "minLength": 1, "description": "附件 URL。" },
        "file_type": { "type": "string", "minLength": 1, "description": "附件类型。" }
      }
    },
    "dedupDebug": {
      "type": "object",
      "additionalProperties": false,
      "required": ["version", "source"],
      "description": "去重调试信息，记录规范化细节与相似度分数。",
      "properties": {
        "version": { "type": "integer", "enum": [4] },
        "source": { "type": "string", "minLength": 1, "description": "去重来源标识。" },
        "source_url": { "type": "string", "minLength": 1, "description": "原始 URL。" },
        "normalized_url": { "type": "string", "minLength": 1, "description": "规范化后的 URL。" },
        "rejected_url_reason": { "type": "string", "minLength": 1, "description": "URL 不参与去重的原因。" },
        "hash_algorithm": { "type": "string", "minLength": 1, "description": "内容哈希算法（如 sha256）。" },
        "text_normalization": { "type": "string", "minLength": 1, "description": "文本规范化方式。" },
        "notice_accession": { "type": "string", "minLength": 1, "description": "SEC accession 编号。" },
        "notice_attachment_url": { "type": "string", "minLength": 1, "description": "公告附件 URL。" },
        "cluster_id": { "type": "string", "minLength": 1, "description": "近似去重簇 ID。" },
        "minhash_threshold": { "type": "number", "description": "MinHash Jaccard 阈值。" },
        "fuzzy_threshold": { "type": "number", "description": "RapidFuzz 正文阈值。" },
        "scores": { "$ref": "#/definitions/nearScores" },
        "reason": { "type": "string", "minLength": 1, "description": "近似去重决策原因。" }
      }
    },
    "nearScores": {
      "type": "object",
      "additionalProperties": false,
      "required": ["minhash_jaccard", "fuzzy_score", "title_score"],
      "description": "近似去重相似度分数。",
      "properties": {
        "minhash_jaccard": { "type": "number", "description": "MinHash Jaccard 相似度（0~1）。" },
        "fuzzy_score": { "type": "number", "description": "正文 RapidFuzz 分数（0~100）。" },
        "title_score": { "type": "number", "description": "标题 RapidFuzz 分数（0~100）。" }
      }
    }
  }
}
